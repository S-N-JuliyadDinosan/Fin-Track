# Fin-Track
The goal is to build a simple, client-side expense tracking application. Users must be able to add income and expenses, view a list of all transactions, see their current balance, and remove transactions. The application must be built using only core HTML, CSS, and JavaScript, with data persisting in the browser's localStorage.
